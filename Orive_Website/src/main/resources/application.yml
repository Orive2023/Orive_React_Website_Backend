spring:
  datasource:
    url: jdbc:mysql://database-1.cmhjntg41ycm.ap-south-1.rds.amazonaws.com:3306/orivehrm
    username: admin
    password: rootroot
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: ORIVEWEBSITE-SERVICE


  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

eureka:
  instance:
    prefer-ip-address: true
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8080
